<Window x:Class="TruckStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="TruckStudio" Height="700" Width="1050" WindowStartupLocation="CenterScreen"
        Background="#0f172a" Foreground="#f8fafc" FontFamily="Segoe UI"
        WindowStyle="None" AllowsTransparency="True" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Icon="pack://application:,,,/Assets/icon.ico">
    
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Margin="10,5">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1e293b"/>
                                <Setter Property="Foreground" Value="#f8fafc"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Action Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3b82f6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2563eb"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Border x:Name="templateRoot" Background="#0f172a" BorderBrush="#334155" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="30" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="1" Background="Transparent" Margin="0" />
                    <materialDesign:PackIcon Grid.Column="1" Kind="ChevronDown" Foreground="#94a3b8" HorizontalAlignment="Center" VerticalAlignment="Center" Width="20" Height="20" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="BorderBrush" TargetName="templateRoot" Value="#3b82f6"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="#f8fafc"/>
            <Setter Property="Background" Value="#0f172a"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid x:Name="templateRoot">
                            <ToggleButton x:Name="toggleButton" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Style="{x:Null}" Template="{StaticResource ComboBoxToggleButton}"/>
                            <ContentPresenter x:Name="contentPresenter" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Content="{TemplateBinding SelectionBoxItem}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Margin="1,-1,-1,-1" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" Placement="Bottom">
                                <Border x:Name="dropDownBorder" Background="#1e293b" BorderBrush="#334155" BorderThickness="1" CornerRadius="6" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=templateRoot}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="#f8fafc"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="#3b82f6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="#0f172a" BorderBrush="#1e293b" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Background="#1e293b" CornerRadius="12,0,0,12" Grid.Column="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Logo Area -->
                    <StackPanel Orientation="Horizontal" Margin="25,30,20,40" Grid.Row="0">
                        <materialDesign:PackIcon Kind="Truck" Width="32" Height="32" Foreground="#3b82f6" VerticalAlignment="Center"/>
                        <TextBlock Text="TruckStudio" Foreground="White" FontSize="22" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Navigation -->
                    <StackPanel Grid.Row="1">
                        <Button Style="{StaticResource SidebarButton}" Click="NavProfile_Click" x:Name="BtnNavProfile">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Account" Width="20" Height="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Player Profile" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SidebarButton}" Click="NavTrucks_Click" x:Name="BtnNavTrucks">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Truck" Width="20" Height="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Trucks &amp; Trailers" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SidebarButton}" Click="NavWorld_Click" x:Name="BtnNavWorld">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Earth" Width="20" Height="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                <TextBlock Text="World &amp; Map" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SidebarButton}" Click="NavTuning_Click" x:Name="BtnNavTuning">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Wrench" Width="20" Height="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Pro Tuning" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SidebarButton}" Click="NavFreight_Click" x:Name="BtnNavFreight">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Briefcase" Width="20" Height="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Freight Market" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    
                    <Button Grid.Row="2" Margin="20" Style="{StaticResource SidebarButton}" Click="CloseApp_Click" Foreground="#ef4444">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Power" Width="20" Height="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Exit" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Main Content Area -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="40,30,40,20">
                    <TextBlock x:Name="PageTitle" Text="Player Profile" FontSize="28" FontWeight="Bold" Foreground="White"/>
                </Grid>

                <!-- Content Pages -->
                <Grid Grid.Row="1" Margin="40,0,40,40">
                    
                    <!-- Page 1: Profile -->
                    <Grid x:Name="PageProfile" Visibility="Visible">
                        <StackPanel>
                            <Border Background="#1e293b" CornerRadius="12" Padding="30" Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="Select Save Game" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,25"/>
                                    
                                    <TextBlock Text="Euro Truck Simulator 2 Profile" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="ProfileComboBox" DisplayMemberPath="ProfileName" Width="350" HorizontalAlignment="Left" Margin="0,0,0,20" SelectionChanged="ProfileComboBox_SelectionChanged"/>
                                    
                                    <TextBlock Text="Save Game" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="SaveComboBox" DisplayMemberPath="SaveName" Width="350" HorizontalAlignment="Left" Margin="0,0,0,30"/>

                                    <Button Content="Load Selected Save" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Click="LoadSave_Click"/>
                                </StackPanel>
                            </Border>

                            <Border Background="#1e293b" CornerRadius="12" Padding="30" x:Name="PanelProfileEdit" IsEnabled="False" Opacity="0.5">
                                <StackPanel>
                                    <TextBlock Text="Player Economy &amp; Progress" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,25"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                        <StackPanel Margin="0,0,40,0">
                                            <TextBlock Text="Money (€)" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                            <TextBox x:Name="TxtMoney" Width="200" Background="#0f172a" Foreground="White" BorderBrush="#334155" Padding="10" FontSize="16" BorderThickness="1"/>
                                        </StackPanel>
                                        <StackPanel>
                                            <TextBlock Text="Experience (XP)" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                            <TextBox x:Name="TxtExperience" Width="200" Background="#0f172a" Foreground="White" BorderBrush="#334155" Padding="10" FontSize="16" BorderThickness="1"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <Button Content="Save Profile Changes" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Click="SaveProfileChanges_Click" Background="#10b981"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <!-- Page 2: Trucks -->
                    <Grid x:Name="PageTrucks" Visibility="Collapsed">
                        <Border Background="#1e293b" CornerRadius="12" Padding="30" VerticalAlignment="Top">
                            <StackPanel x:Name="PanelTrucksEdit" IsEnabled="False" Opacity="0.5">
                                <TextBlock Text="Fleet Maintenance &amp; Actions" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,25"/>
                                
                                <TextBlock Text="Instantly repair all your trucks and trailers to 100% condition." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <Button Content="Fix All Trucks &amp; Trailers" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Click="FixTrucks_Click" Background="#f59e0b" Padding="30,15" Margin="0,0,0,20"/>
                                
                                <TextBlock Text="Refuel all your trucks to 100% (without repairing)." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <Button Content="Refill Fuel (100%)" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Click="RefillFuel_Click" Background="#10b981" Padding="30,15" Margin="0,0,0,20"/>

                                <TextBlock Text="Fix the cargo damage of your active delivery back to 0%." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <Button Content="Fix Cargo Damage (0%)" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Click="FixCargo_Click" Background="#8b5cf6" Padding="30,15" Margin="0,0,0,20"/>

                                <TextBlock Text="Teleport (Camera 0)" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,10,0,10"/>
                                
                                <StackPanel x:Name="PanelTeleportDisabled" Visibility="Collapsed" Margin="0,0,0,20">
                                    <TextBlock Text="Requirement: Camera 0 not enabled!" Foreground="#f43f5e" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="You must activate g_console and g_developer (set them to 1) in the config.cfg file located in your Documents folder." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                </StackPanel>

                                <StackPanel x:Name="PanelTeleportEnabled" Margin="0,0,0,20">
                                    <TextBlock Text="Instructions: Save the game and press Alt + F12, as Truck Tools does." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Teleport" Style="{StaticResource PrimaryButton}" Click="Teleport_Click" Background="#8b5cf6" Padding="20,10"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Page 3: World & Map -->
                    <Grid x:Name="PageWorld" Visibility="Collapsed">
                        <Border Background="#1e293b" CornerRadius="12" Padding="30" VerticalAlignment="Top">
                            <StackPanel x:Name="PanelWorldEdit" IsEnabled="False" Opacity="0.5">
                                <TextBlock Text="World &amp; Map Exploits" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,25"/>
                                
                                <TextBlock Text="World &amp; Map Exploits" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,25"/>
                                
                                <!-- Visit All Garages -->
                                <TextBlock Text="Unlock every city without buying garages. Sets all undiscovered garages to the minimum (Small) so you can upgrade manually." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <Button Content="Visit All Garages" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,0,0,20" Background="#0ea5e9" Padding="30,15" Click="VisitGarages_Click"/>

                                <!-- Upgrade Owned Garages -->
                                <TextBlock Text="Upgrade every garage you already own to maximum size (6 slots). Does not buy garages you don't own yet." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <Button Content="Upgrade All Owned Garages" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,0,0,20" Background="#10b981" Padding="30,15" Click="UpgradeGarages_Click"/>

                                <!-- Buy All Garages -->
                                <TextBlock Text="Purchase and fully upgrade every garage to maximum size (6 slots) across the entire map." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                <Button Content="Buy All Garages" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Background="#f43f5e" Padding="30,15" Click="BuyAllGarages_Click"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Page 4: Tuning -->
                    <Grid x:Name="PageTuning" Visibility="Collapsed">
                        <Border Background="#1e293b" CornerRadius="12" Padding="30" VerticalAlignment="Top">
                            <StackPanel x:Name="PanelTuningEdit" IsEnabled="False" Opacity="0.5">
                                <TextBlock Text="Pro Tuning &amp; Cheats" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,25"/>
                                
                                <TextBlock Text="Enable infinite fuel or completely max out all driver skills (ADR, Long Distance, etc)." Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,0,0,20"/>
                                
                                <Button Content="Max Out All Skills" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,0,0,10" Background="#10b981" Padding="30,15" Click="MaxSkills_Click"/>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <Button Content="Extended Fuel (Set km)" Style="{StaticResource PrimaryButton}" Background="#fbbf24" Foreground="Black" Padding="30,15" Click="InfiniteFuel_Click" Margin="0,0,10,0"/>
                                    <Button Content="Restore Fuel" Style="{StaticResource PrimaryButton}" Background="#3b82f6" Padding="30,15" Click="RestoreFuel_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Page 5: Freight Market -->
                    <Grid x:Name="PageFreight" Visibility="Collapsed">
                        <Border Background="#1e293b" CornerRadius="12" Padding="30" VerticalAlignment="Top">
                            <StackPanel x:Name="PanelFreightEdit" IsEnabled="False" Opacity="0.5">
                                <TextBlock Text="Custom Job Generator" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,25"/>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <StackPanel Margin="0,0,40,0">
                                        <TextBlock Text="Source City" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="CbSourceCity" Width="300" HorizontalAlignment="Left" SelectionChanged="CbSourceCity_SelectionChanged"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="Source Company" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="CbSourceCompany" Width="300" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <StackPanel Margin="0,0,40,0">
                                        <TextBlock Text="Destination City" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="CbDestCity" Width="300" HorizontalAlignment="Left" SelectionChanged="CbDestCity_SelectionChanged"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="Destination Company" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="CbDestCompany" Width="300" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                    <StackPanel Margin="0,0,40,0">
                                        <TextBlock Text="Cargo" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="CbCargo" Width="300" HorizontalAlignment="Left"/>
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock Text="Urgency" Foreground="#94a3b8" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="CbUrgency" Width="150" HorizontalAlignment="Left">
                                            <ComboBoxItem Content="Normal" IsSelected="True"/>
                                            <ComboBoxItem Content="Important"/>
                                            <ComboBoxItem Content="Urgent"/>
                                        </ComboBox>
                                    </StackPanel>
                                </StackPanel>

                                <Button Content="Inject Custom Job" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Click="InjectJob_Click" Background="#8b5cf6" Padding="30,15"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
            
            <!-- Version Number -->
            <TextBlock Text="v0.1.5-alpha" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,15,15" Foreground="#64748b" FontSize="12" IsHitTestVisible="False" Panel.ZIndex="10"/>
        </Grid>
    </Border>
</Window>
